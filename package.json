{
  "name": "velocity-root",
  "private": true,
  "scripts": {
    "dev": "cls && npx kill-port 8080 && concurrently --names \"go,vite\" --prefix-colors \"cyan,magenta\" \"cd backend && air\" \"cd frontend && npm run dev\"",
    "fe": "cls &&  npx kill-port 5173 && cd frontend && npm run dev",
    "be": "cls && npx kill-port 8080 && cd backend && air",
    "kill": "cls && npx kill-port 8080",
    "restart": "cls && npx kill-port 8080 && cd backend && air"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "shadcn": "^4.7.0"
  }
}
